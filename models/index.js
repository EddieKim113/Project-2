// Dependencies

// Sequelize for standard library
var Sequelize = require("sequelize");
// sequelize for connection to DB
var sequelize = require("../config/connect.js");

var Product = sequelize.define()
